{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.12"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "chmod +x node_modules/.bin/vitepress && vitepress build docs",
    "docs:preview": "vitepress preview"
  }
}